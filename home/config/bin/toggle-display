#!/usr/bin/env bash
set -u

output_name="eDP-1"

fail() {
  message="toggle-display: $*"
  printf '%s\n' "$message" >&2
  logger -t toggle-display -- "$message"
  exit 1
}

sleep 1
wlopm --off "$output_name" || fail "could not power off $output_name"
