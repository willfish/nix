#!/usr/bin/env bash
set -u

output_name="eDP-1"

fail() {
  message="toggle-display: $*"
  printf '%s\n' "$message" >&2
  logger -t toggle-display -- "$message"
  exit 1
}

wlopm --toggle "$output_name" || fail "could not toggle $output_name"
