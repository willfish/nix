# /home/william/.config/tmuxinator/backend.yml

name: work
windows:
  - 📀:
      panes:
        - cd ~/Repositories/hmrc/trade-tariff-backend && eval "$(direnv export fish)" && pg-start
        - cd ~/Repositories/hmrc/trade-tariff-dev-hub && eval "$(direnv export fish)" && pg-start
        - cd ~/Repositories/hmrc/trade-tariff-admin && eval "$(direnv export fish)" && pg-start
        - valkey-server
        - cd ~/Repositories/hmrc/trade-tariff-backend && eval "$(direnv export fish)" && opensearch-start
